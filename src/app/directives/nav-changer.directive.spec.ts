import { NavChangerDirective } from './nav-changer.directive';

describe('NavChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new NavChangerDirective();
    expect(directive).toBeTruthy();
  });
});
